@model IEnumerable<fBlockBuster.Models.tblRating>

@{
    ViewBag.Title = "Index";
}


@{

    if (Convert.ToInt32(Session["Tipo"]) == 1)
    {
        Layout = "~/Views/Shared/Admin.cshtml";

    }
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Rating)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.idRating }) |
                @Html.ActionLink("Details", "Details", new { id = item.idRating }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.idRating })
            </td>
        </tr>
    }

</table>
