@model IEnumerable<fBlockBuster.Models.tblGenero>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Genero)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Genero)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idGenero }) |
            @Html.ActionLink("Details", "Details", new { id=item.idGenero }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idGenero })
        </td>
    </tr>
}

</table>
